name: autofix.ci (legacy)

on:
  workflow_dispatch:
permissions:
  contents: read

jobs:
  autofix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Legacy placeholder
        run: echo "Use .github/workflows/gofmts.yml for automated formatting fixes."